<template>
	<div class="question" @click="isOpen = !isOpen">
		<div class="question-title">{{ question.title }}</div>
		<div class="question-text" v-if="isOpen">{{ question.text }}</div>
	</div>
</template>

<script>
import { ref } from 'vue'


export default {
	props: ['question'],
	setup() {
		const isOpen = ref(false)
		return {isOpen}
	}
}
</script>

<style lang="scss" scoped>

</style>